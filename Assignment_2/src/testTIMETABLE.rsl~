NET

/* The following class is composed of a set of unit tests made on functions used 
 * in the Scheme NET.
 */
scheme testTIMETABLE =
extend NET with 
	class
	
				
	test_case
	
	[addTram_1] 
			let
				trId = "TramA",
				tt = {},			
			in 
				card addTram(trId, tt) = 1
			end
end